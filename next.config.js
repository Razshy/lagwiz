/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    experimental: {
      appDir: true, // Ensure this is enabled for Next.js projects using the `app` directory.
    },
  };
  